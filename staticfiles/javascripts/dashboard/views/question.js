define(['app'], function(App) {
    
    App.Pages.Question = new (Amour.PageView.extend({
        events: {},
        initPage: function() {},
        render: function() {}
    }))({el: $('#view-question')});
    
});
